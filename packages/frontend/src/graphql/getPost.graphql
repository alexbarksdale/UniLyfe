query GetPost($postId: Int!) {
    getPost(postId: $postId) {
        id
        thumbnail
        title
        content
        likes
        views
        createdAt
        category {
            id
            name
        }
        author {
            id
            username
            email
            universityName
        }
    }
}
