query GetPost($postId: Int!) {
    getPost(postId: $postId) {
        id
        title
        content
        likes
        views
        createdAt
        category {
            id
            name
        }
        author {
            id
            username
        }
    }
}
