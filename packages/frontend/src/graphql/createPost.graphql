mutation CreatePost($email: String!, $title: String!, $content: String!) {
    createPost(email: $email, title: $title, content: $content) {
        id
        title
        content
        likes
        views
        createdAt
        author {
            id
            username
            email
        }
    }
}
