mutation CreatePost(
    $authorId: Float!
    $title: String!
    $content: String!
    $categoryName: String!
) {
    createPost(
        authorId: $authorId
        title: $title
        content: $content
        categoryName: $categoryName
    ) {
        id
        title
        content
        likes
        views
        createdAt
        author {
            id
            username
            email
        }
    }
}
