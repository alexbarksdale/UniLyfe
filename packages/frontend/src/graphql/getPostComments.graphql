query GetPostComments($postId: Int!) {
    getPostComments(postId: $postId) {
        id
        postId
        content
        replyId
        author {
            id
            username
            universityName
        }
        createdAt
    }
}
